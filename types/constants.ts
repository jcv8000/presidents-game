import { NIL as NIL_UUID } from "uuid";

export const UUID_LENGTH = NIL_UUID.length;

export const ROOM_CODE_SIZE = 4;
export const ROOM_CODE_ALPHABET = "abcdefghijklmnopqrstuvwxyz";

/**
 * Number of days the cookie lasts before it expires
 */
export const COOKIE_EXPIRATION = 1;
/**
 * Number of days the cookie lasts before it expires
 */
export const NAME_COOKIE_EXPIRATION = 365;

export const ARTIFICIAL_CONNECT_DELAY = 150;

export const ROOM_SIZE_LIMIT = 8;
